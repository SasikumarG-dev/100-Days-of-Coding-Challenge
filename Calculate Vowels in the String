// Java program to Calculate the Vowel words in the given String

package calculatevowelsinstrings;
public class CalculateVowelsInStrings {
    public static void main(String[] args) {
        String str="Welcome to my GitHub profile";int count=0;
        str=str.toLowerCase();
        for(int i=0;i<str.length();i++){
            if(str.charAt(i)=='a'||str.charAt(i)=='e'
                    ||str.charAt(i)=='i'||str.charAt(i)=='o'||str.charAt(i)=='u'){
                count++;
            }
        }
        System.out.println("The number of vowels in the given string is "+count);
    }
}

OUTPUT:
The number of vowels in the given string is 9
