package missingnumber;
public class MissingNumber {
    public static void main(String[] args) {
        int arr[]={1, 2, 4, 6, 3, 7, 8};int sum=0,sum1=0;
        for(int i=1;i<=arr[arr.length-1];i++){
            sum+=i;
        }
        for(int i=0;i<arr.length;i++){
            sum1+=arr[i];
        }
        System.out.println("The number missing from 1 to "+arr[arr.length-1]+" is "+(sum-sum1));
    }
}

OUTPUT:
The number missing from 1 to 8 is 5
